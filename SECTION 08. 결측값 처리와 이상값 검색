SECTION 08. 결측값 처리와 이상값 검색
· 결측 데이터(결측값) : 누락된 데이터 및 비어있는 데이터
		      완전 무작위 결측 / 무작위 결측 / 비 무작위 결측
	- 완전 무작위 결측 : 변수 상에 발생한 결측값이 다른 변수들과 전혀 관계가 없는 경우
	- 무작위 결측 : 특정 변수와 관련이 있지만 그 변수의 결과는 관계가 없는 경우
	- 비 무작위 결측 : 누락된 변수 결과가 다른 변수와 관련이 있는 경우

· 결측값 처리 방법
 	- 명시적 모형에 의한 대체 : 확률분포를 따른다고 가정한 후에 분포의 모수들을 추정하여 대체
	- 내재적 모형에 의한 대체 : 확률분포를 따른다고 가정하지 않고 가능한 정확한 값을 가지고 대체
	- 혼합된 대체

· 데이터 이상값 : 관측된 데이터의 범위에서 벗어나는 아주 큰 값 혹은 아주 작은 값을 의미
		평균을 왜곡시키기 때문에 제거해야 한다.

· 상자그림은 사분위수 데이터를 나타낸다.
